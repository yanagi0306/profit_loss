<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">店舗ログイン</h2>

      <%= form_with  model:@store,url:store_session_path, local: true do |f| %>

      <div class="field">
        <%= f.label :name , "店舗名" ,class:"certification"%><br />
        <%= f.text_field :name, id:"user_email", autofocus: true, autocomplete: "name" %>
      </div>

      <div class="field">
        <%= f.label :password, "パスワード（6文字以上）" ,class:"certification"%><br />
        <%= f.password_field :password, id:"user_password", autocomplete: "current-password" %>
      </div>
      <% unless company_signed_in? %>
      <%= link_to '会社でログイン',new_company_session_path, class: "info"%><br>

      <%= link_to '会社新規登録',new_company_registration_path, class: "info"%>
      <%end%>
      <div class="actions">
        <%= f.submit "ログイン", class: :form__btn %>
      </div>
      <% end %>
    </div>
  </div>
</div>
